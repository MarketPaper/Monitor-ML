# WORKFLOW DESHABILITADO - Usar script local ./deploy.sh en su lugar
# Spaceship solo soporta FTP, lo que causa timeouts desde GitHub Actions
# Ver README.md para instrucciones de deploy manual

name: Deploy Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deploy manual: ejecutar ./deploy.sh localmente"
